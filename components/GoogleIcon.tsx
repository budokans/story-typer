import { FcGoogle } from "react-icons/fc";
import { Icon } from "@chakra-ui/icons";
import { ReactElement } from "react";

export const GoogleIcon = (): ReactElement => (
  <Icon as={FcGoogle} mr={2} h={5} w={5} />
);
