import { LayoutContainer } from "@/containers/layout";
import { GameContainer } from "@/containers/game";

const Play: React.FC = () => {
  return (
    <LayoutContainer>
      <GameContainer />
    </LayoutContainer>
  );
};

export default Play;
